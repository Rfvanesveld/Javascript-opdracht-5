let person = {
    name: 'Richard',
    age: 47
};

// Dot notation
person.name = 'Frans'

// Bracket notation

console.log(person.name);
console.log(person.age);

let selectie = 'name';
person[selectie] = 'Jari';
person[2] = 'getrouwd';

let evaluations = [7, 10, 9];
console.log(evaluations)
